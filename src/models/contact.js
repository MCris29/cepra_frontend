export class Contact {
    constructor(
        itcon_nombre, 
        itcon_email, 
        itcon_nivel_estudios, 
        itcon_nivel_decision
    ) {
        this.itcon_nombre = itcon_nombre;
        this.itcon_email = itcon_email;
        this.itcon_nivel_estudios = itcon_nivel_estudios;
        this.itcon_nivel_decision = itcon_nivel_decision;
    }
}