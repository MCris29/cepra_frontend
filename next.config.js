/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

const pathsConfig = {};

module.exports = {
  nextConfig,
  trailingSlash: true,
  // assetPrefix: "/o2i/",
  // eslint: {
  //   ignoreDuringBuilds: true,
  // },
};
