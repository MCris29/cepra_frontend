/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
};

module.exports = {
  nextConfig,
  trailingSlash: true,
  basePath: "/o2i",
  // eslint: {
  //   ignoreDuringBuilds: true,
  // },
};
